# 求解器问题记录

> 基于代码审查与教师示例对比，记录所有已知问题。
> 代码位置：`backend/app/solvers/{slab,beam,main_beam}/`
> 规范：SL 191-2008 / GB 50010

---

## 修复状态（2026-06-28 复核后）

| # | 状态 | 说明 |
|---|---|---|
| 1, 2, 13 | ✅ 已修复 | 板跨度方向改为 `width`(L₂)。问题 1 修复后 `width` 字段被实际使用，门禁必填正确 |
| 8, 10 | ✅ 已修复 | `calc_envelope` 区分 l0_edge(M1 边跨轴线)/l0_interior(M2 中跨及 M_B/M_C 支座，1.05×(轴线−柱宽))；V₀ 改用简支剪力 (G+Q)/2。经教师平台实测数据全量复核：**板、次梁荷载/内力/弯矩逐位吻合**，主梁 M1=203.0436/M2=96.36/M_B=−206.6862 吻合（⚠️ 文档原称 M2=97.45 系误判，已订正为 96.36） |
| 9 | ◯ 非缺陷 | 课程简化全 T 形，代码本就正确（此前误判源于独立脚本用 b=300） |
| 3 | ✅ 已删除 | `wall_thickness` 冗余字段已移除（`BeamInput` 由 `/calculate` 派生，不入库，无序列化风险） |
| 4 | ⏸ 暂不动 | `spans` 冗余字段无功能影响 |
| 5, 6, 7 | ⏸ 阻塞 | 求解器**无荷载推导代码**（`calculate_main_beam_load` 仅对入参求和）；属 Task 9，几何(h/b/板厚)待同组确认。修正分量已固化进 `test_main_beam.py`（59.4228/7.2/0.4896/48.0） |
| 11 | 🔵 待定 | 斜截面 h₀/fc 仍需教师确认 |
| 12 | ✅ 已修复 | beam/main_beam 已接通 `/calculate`（全链集成，Task 11），三页均返回 200 |

> V₀ 取值（原 open-engineering-issues 第 1 项）已由教师示例拍板：**简支剪力 (G+Q)/2**。

---

## 坐标系约定（L₁/L₂）

楼盖平面定义：

| 方向 | 平行关系 | 代码字段 | 涉及构件 |
|---|---|---|---|
| **L₁** | 与 **次梁** 平行 | `SlabInput.length` | 次梁跨度方向；板的长方向 |
| **L₂** | 与 **主梁** 平行 | `SlabInput.width` | 主梁跨度方向；板的跨度方向(短向)；次梁间距方向 |

---

## 一、板求解器 (Slab) — `solvers/slab/solver.py`

### 问题 1：跨度方向用错（L₁ 当作 L₂）

- **位置**：`solver.py:72,89`（`calculate_spans`、`calculate_net_spans`）
- **当前代码**：
  ```python
  middle_span = inp.length / inp.spans          # 用了 length (L₁)
  span_unit = inp.length / inp.spans            # 用了 length (L₁)
  ```
- **正确做法**：
  ```python
  middle_span = inp.width / inp.spans           # 应用 width (L₂)
  span_unit = inp.width / inp.spans
  ```
- **原因**：板以次梁为支座，跨度沿 **L₂** 方向。`length` (L₁) 平行于次梁，不是跨度方向。
- **影响**：板跨度计算错误，同时导致 `width` 标记必需却从未使用。

### 问题 2：`width` 门禁标记必需但未使用

- **位置**：`models/slab.py:23` + `models/project.py:9`（`SLAB_REQUIRED_INPUT`）
- **说明**：`width` 被 `SLAB_REQUIRED_INPUT` 标记为必填，但因为问题1，求解器从未引用它。
- **修复后状态**：问题1修复后此问题自动消除。

---

## 二、次梁求解器 (Beam) — `solvers/beam/solver.py`

### 问题 3：`wall_thickness` 多余

> ✅ **已解决**：字段已从 `BeamInput` 移除（2026-06-29 代码整理）。以下为历史记录。

- **位置**：`models/beam.py:30`（历史）
- **说明**：有默认值 `370`，但求解器全程未引用。`BeamInput` 中此参数无用。
- **影响**：无，仅模型冗余。

---

## 三、主梁求解器 (Main Beam) — `solvers/main_beam/solver.py`

### 问题 4：`spans` 多余

- **位置**：`models/main_beam.py:26`
- **说明**：有默认值 `3`，但求解器硬编码为三等跨连续梁，忽略输入值。
- **影响**：如需支持非三跨主梁，需扩展。

### 问题 5：主梁自重取了全截面

- **位置**：`solver.py:22`（`calculate_main_beam_load` 参数）
- **当前计算**：
  ```python
  self_weight = 25.0 × (b/1000) × (h/1000) × s_beam
  # = 25 × 0.3 × 0.6 × 2.0 = 9.0 kN
  ```
- **正确做法**：应扣除板厚部分（仅腹板）：
  ```python
  dh = (h - slab_h) / 1000                       # 腹板高 = 0.48m
  self_weight = 25.0 × (b/1000) × dh × s_beam
  # = 25 × 0.3 × 0.48 × 2.0 = 7.2 kN
  ```
- **原因**：板厚范围内的自重已由板计算承担，主梁只承担板以下的腹板部分。

### 问题 6：次梁传主梁的恒载集中力算法错误

- **当前方法**：用连续梁剪力系数（`beta`）乘标准值计算次梁内支座反力。
- **正确做法**：用简支总荷载 `w × L`：
  ```python
  G_sec_beam = g_std_beam × span_beam
  # = 9.9038 × 6.0 = 59.42 kN
  ```
- **差异**：旧值 63.90 kN → 正确值 59.42 kN。
- **原因**：主梁计算只需次梁端部反力（总荷载），不需要连续梁系数。

### 问题 7：活载集中力算法错误

- **当前方法**：用连续梁剪力系数，得到 55.54 kN。
- **正确做法**：用从属面积法：
  ```python
  Qk = Q_live × (span_beam × s_beam)
  # = 4.0 × (6.0 × 2.0) = 48.00 kN
  ```
- **差异**：旧值 55.54 kN → 正确值 48.00 kN。
- **原因**：次梁传给主梁的集中力 = 面荷载 × 从属面积，不应使用连续梁系数。

### 问题 8：计算跨度取值错误（✅ 已修复，规则经教师平台数据订正）

- **位置**：`solver.py:51-52`（`calculate_main_beam_internal_forces`）+ `utils.py:calc_envelope`
- **原代码**：`calc_envelope(G, Q, l0=span)` 单一 l0=6.0 用于所有截面。
- **订正后规则**（按教师平台"内力系数表"的 span_m 列）：
  | 截面 | 性质 | 用哪个 L | 取值 |
  |---|---|---|---|
  | M1 | **边跨**跨中 | L₁ = 轴线跨 | 6.000m |
  | M2 | **中跨**跨中 | L₂ = 1.05×(轴线−柱宽) | 5.9325m |
  | M_B, M_C | 支座 | L₂ = 1.05×(轴线−柱宽) | 5.9325m |
- **实现**：`calc_envelope(G, Q, l0_edge, l0_interior)`，仅 M1 用 l0_edge，M2/M_B/M_C 用 l0_interior。
- **⚠️ 订正说明**：本文档最初把 M2 归为"跨中用 L₁=6.0"得 97.4481，**这是错的**。教师平台 M2 的 span_m=5.933，M2=96.36。区别在于 M1 是**边跨**、M2 是**中跨**，而非"跨中 vs 支座"。

### 问题 9：支座截面矩形/T形判断错误

- **位置**：`solver.py:107-108`
- **当前代码**：
  ```python
  flexure_sections = [
      ("1", forces.M1_max, True),          # 跨中用 T形
      ("B", abs(forces.M_B_min), True),     # 支座也用 T形 (但 bf 未正确传入)
      ("2", forces.M2_max, True),
  ]
  ```
- **当前 `calc_main_beam_flexure` 实现**：
  ```python
  def calc_main_beam_flexure(..., use_t_section=True):
      if use_t_section:
          bw = bf    # 用翼缘宽
      else:
          bw = b     # 用腹板宽
  ```
- **问题**：
  - 主梁课程简化规定：**全部截面按 T形（翼缘宽 bf）计算**，包括支座。
  - 但当前代码的 `calc_main_beam_flexure` 在 `use_t_section=True` 时确实用了 `bf`，**B支座应传入 bf=2000，当前传入的 bf 正确**。
  - 但在之前我们独立测试时（`run_main_beam_full.py`），我错误地用了 `b=300` 做矩形截面，导致 As_need=1766 而非 1508。
- **结论**：代码实现逻辑正确，但使用时应确认 `bf` 参数正确传入。

### 问题 10：支座边缘调整 V₀ 用错

- **位置**：`solver.py:65-66`
- **当前代码**：
  ```python
  va = env["VA"]                                            # 包络端支座剪力
  m_b_adj = env["M_B"] + (bw / 2.0) * va                   # 用了 VA
  m_c_adj = env["M_C"] + (bw / 2.0) * va                   # 用了 VA
  ```
- **正确做法**：
  ```python
  V0 = (G + Q) / 2.0                                        # 简支剪力
  m_b_adj = env["M_B"] + (bw / 2.0) * V0
  m_c_adj = env["M_C"] + (bw / 2.0) * V0
  ```
- **差异对比**：
  | 项目 | 旧值（用 VA=101.89） | 正确（用 V₀=64.03） |
  |---|---|---|
  | M_B | -203.38 | **-206.69** |
  | M_C | -129.40 | **-135.95** |
- **原因**：支座边缘调整应使用该支座处简支梁剪力 `V₀`，而非端支座包络值 `VA`。

### 问题 11（疑似）：斜截面 Vc 的 fc / h0 取值（🔵 待同学确认）

教师平台 Vc 反推得到的约定，**次梁与主梁不一致**，无法在代码里统一成一条规则，故求解器暂维持 `Vc=0.07·fc·b·h0/γd`（fc=9.6，h0=h−c−d/2），待确认：

| 构件 | 教师 Vc | 反推约定 | 求解器值 |
|---|---|---|---|
| 次梁 (200×500) | 59.0333 | **fc=11**（旧规范 fcm），h0=460（h−c−d/2） | 51.5（fc=9.6） |
| 主梁 (300×600) | 92.4 | **fc=9.6**，h0=550（h−c−d，非 d/2） | 94.08（h0=560） |

- 次梁斜截面疑似用旧规范弯曲抗压强度 fcm=11；主梁用 fc=9.6 但 h0 取 h−c−d（少算半个纵筋直径）。
- 正截面（fc=9.6）三个构件均与教师逐位吻合，仅斜截面 Vc 有此分歧。
- **建议**：向同学/教师确认斜截面统一用 fc=9.6 还是 fcm=11，以及 h0 取 h−c−d/2 还是 h−c−d。

---

## 四、API 层问题

### 问题 12：次梁/主梁 API 未接通

> ✅ **已解决**（全链集成，2026-06-29）：三页 `/calculate` 均已接通，返回 200。以下为历史记录。

- **位置**：`api/projects.py:101-106`（历史）
- **说明**：`POST /api/projects/{id}/calculate` 只有 `page="slab"` 挂接，`beam` 和 `main_beam` 返回 501 Not Implemented。
- **影响**：次梁和主梁求解器代码和测试已就绪，但无法通过 API 调用。

### 问题 13：slab 输入校验 `width` 必填

- **位置**：`api/projects.py:109` + `models/project.py:9`
- **说明**：`SLAB_REQUIRED_INPUT` 包含 `width`，但求解器不需要（见问题1-2）。
- **影响**：用户必须输入 `width` 才能调 slab API，但输入后不会被使用。

---

## 五、与教师示例核对表

以下为按手稿/教师示例修正后，三个内力截面值：

| 截面 | 教师值 | 修正公式 |
|---|---|---|
| M1 | 203.0436 | `(0.244·70.468 + 0.289·57.600) × 6.0000`（边跨，轴线） |
| M_B(边缘) | -206.6862 | `[-0.267·70.468 + (-0.311)·57.600] × 5.9325 + (0.35/2)·64.03` |
| M2 | 96.36 | `(0.067·70.468 + 0.200·57.600) × 5.9325`（中跨，1.05×ln） |

荷载参数：

| 参数 | 值 | 来源 |
|---|---|---|
| Gk | 67.1124 kN | 59.4228(次梁) + 7.200(自重) + 0.4896(粉刷) |
| Qk | 48.0000 kN | 4.0 × 6.0 × 2.0 |
| G | 70.4680 kN | 67.1124 × 1.05 |
| Q | 57.6000 kN | 48.0000 × 1.20 |

---

## 六、优先级建议

| 优先级 | 问题编号 | 影响 | 状态 |
|---|---|---|---|
| 🔴 高 | 1, 10 | slab 跨度算错 + 主梁内力算错 | ✅ 已修复 |
| 🔴 高 | 8, 9 | 主梁 M_B/M2 算错（9 经核为非缺陷） | ✅ 已修复 |
| 🟡 中 | 5, 6, 7 | 主梁荷载值偏差 | ⏸ 阻塞（几何待确认） |
| 🟡 中 | 12 | 次梁/主梁无法 API 调用 | ⏸ 越界 |
| 🟢 低 | 2, 3, 4 | 冗余参数 | ◯ 2 随 1 消除；3、4 暂留 |
| 🔵 待定 | 11 | 需确认教师 h0 取值 | 🔵 待定 |
